require 'greetings_langs'

#This is the test suite
describe do
  #Before every single test it will do this. Or you can do before all to do it before everything
  before(:each) do
    #Instatiate the object
    @greetings =
  end

  


end
